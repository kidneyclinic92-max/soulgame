"use client";

// WalletModal is now handled by Reown AppKit's built-in modal.
// The modal is triggered via useAppKit().open() from the WalletButton.
// No custom modal needed — AppKit provides QR code, social login, and more.

export default function WalletModal() {
  return null;
}
